# netlify.toml

[build]
  command = "npm run build"
  publish = "dist"

[functions]
  included_files = ["backend/**"]

# Make sure this [[redirects]] block is at the VERY BOTTOM of the file
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api/:splat"
  status = 200
  force = true